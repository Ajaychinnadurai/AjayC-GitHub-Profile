# Ajay C — GitHub Profile README

1. Create a public repository named exactly `Ajaychinnadurai`.
2. Upload `README.md`.
3. Upload the `assets` folder and keep `assets/header.svg` at the same path.
4. Commit the changes.
5. Open your GitHub profile to see the new profile section.

The README uses a local custom SVG hero plus external stat/icon services.
